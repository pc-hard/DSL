pipeline {
    agent none
    stages {
        stage('checkout HELLO PY ?') {
            steps {
                echo "I don't need no node"
            }
        }
        stage('test') {
             agent {
                 label 'master'
             }
             steps {   
                
                sh 'echo from master'
                 definition {
        cps {
            script(readFileFromWorkspace('test2'))
    	    sandbox()
}
            }
        }
        stage('deploy') {
            agent {
                label 'master'
            }
           
            steps {
                sh 'ls -l'
            }
        }
    }
}
}
