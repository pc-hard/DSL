#!groovy
def releaseScript = readFileFromWorkspace('test2')
pipeline {
    agent none
    stages {
                stage('test') {
             agent {
                 label 'master'
             }
             steps {   
        groovyCommand(releaseScript)
    	  }
            }
        }
   
