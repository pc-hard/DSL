pipeline {
    agent none
    stages {
        stage('checkout HELLO PY ?') {
            steps {
                echo "I don't need no node"
            }
        }
        stage('test') {
             agent {
                 label 'master'
             }
             steps {   
                
                sh 'echo from master'
groovyCommand (readFileFromWorkspace('test2'))
    	    
}
            }
        }
        stage('deploy') {
            agent {
                label 'master'
            }
           
            steps {
                sh 'ls -l'
            }
        }
    }
